# Core web framework and HTTP client
Flask==2.3.3
gunicorn==21.2.0
requests==2.31.0

# Encryption (required for AES-256-GCM payload encryption)
cryptography==41.0.7

# Optional exfiltration channels (install if you use them)
dnspython==2.4.2          # DNS exfil
boto3==1.34.0             # S3 exfil (also requires AWS credentials)
websocket-client==1.6.3   # WebSocket exfil
paho-mqtt==1.6.1          # MQTT exfil
